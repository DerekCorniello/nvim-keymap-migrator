// Loads and applies nvim → editor action mappings

export function loadMappings() {
  // TODO: Load default-mappings.json
}

export function lookupMapping(keymap, editor) {
  // TODO: Return mapped command for given editor
}
